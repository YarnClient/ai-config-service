server:
  address: 0.0.0.0
  port: 9898

# ---- HTTPS（服务器部署时取消注释，并确保 keystore.p12 已放入 src/main/resources/）----
#  ssl:
#    enabled: true
#    key-store: classpath:keystore.p12
#    key-store-password: changeit
#    key-store-type: PKCS12
#    key-alias: selfsigned

spring:
  application:
    name: ai-config-service
